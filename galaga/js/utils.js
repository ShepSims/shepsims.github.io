// utilities.js

// Utility function for random numbers within a range
function randomRange(min, max) {
	return Math.random() * (max - min) + min;
}

// If you have other utility functions, you can add them here as well.
